import jugador.*
import item.*
import nivel.*
import direcciones.*
import config.*
import wollok.game.*

describe "Tests de jugador" {

	method initalize() {
		config.nivelActual(nivel1)
		jugador.position(game.at(0, 0))
	}

	test "El jugador no puede atravesar una pared que tiene enfrente" {
		nivel1.agregarParedX(1, 1, 0)
		jugador.position().right(1) // el test no funciona con jugador.mover(derecha)
		assert.equals(jugador.position(), game.at(0, 0))
	}
	
	/* 
	test "El jugador puede agarrar un item que tiene enfrente" {
		nivel1.crearMueble(1,0,"silla_izquierda.png")
		jugador.mover(derecha)
		jugador.agarrarItem()
		assert.that(jugador.tieneItem())
	}*/

}

